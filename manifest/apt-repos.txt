# Third-party apt repositories.
# Each section: [name], then key_url, key_file, key_dearmor, list, packages fields.
# key_dearmor = yes: pipe key through gpg --dearmor (ASCII-armored keys).
# key_dearmor = no:  download key directly (already binary GPG format).
# {arch} expands to $(dpkg --print-architecture) at install time.
# {key_file} expands to the key_file value.

[github-cli]
key_url     = https://cli.github.com/packages/githubcli-archive-keyring.gpg
key_file    = /usr/share/keyrings/githubcli-archive-keyring.gpg
key_dearmor = no
list        = deb [arch={arch} signed-by={key_file}] https://cli.github.com/packages stable main
packages    = gh

[vscode]
key_url     = https://packages.microsoft.com/keys/microsoft.asc
key_file    = /usr/share/keyrings/microsoft-archive-keyring.gpg
key_dearmor = yes
list        = deb [arch={arch} signed-by={key_file}] https://packages.microsoft.com/repos/code stable main
packages    = code
