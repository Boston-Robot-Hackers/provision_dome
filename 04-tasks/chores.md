# Chores

- [x] Fix `scripts/dome-config.sh` resolving `manifest/` one level above the repo root when `source`d from zsh — `${BASH_SOURCE[0]}` isn't reliably populated by zsh on plain `source`, so `dirname` fell back to cwd (repo root) and `../manifest` landed a level too high (`ERROR: 'DOME_USER' not set in .../dome-docker/../manifest/config.txt`). Switched to resolving `manifest/` from `$(pwd)` (matches the already-documented "cd to repo root first" usage) with a clear error if `manifest/config.txt` isn't found there. Regression test: `tests/test_f01_manifest.sh` "dome-config.sh sourced under zsh" check.
- [x] `scripts/host-setup.sh` now prints the target's global IPv4 addresses (`ip -4 -o addr show scope global`) just before finishing, so VM users (where `dome.local` mDNS often doesn't resolve) don't have to find the IP themselves some other way. Updated `02-doc/shell-howto.md` and `02-doc/docker-howto.md` "SSH back in" steps to reference this output instead of telling the user to look the IP up externally. No automated test — `ip` is Linux-only (the script only ever runs on the Pi/VM target, never on the Mac dev machine), so this is manual/visual verification during T10 (real VM/Pi bring-up).
- [x] Fix `scripts/host-setup.sh` resolving `DOME_USER` as `root` — it only read the `DOME_USER` env var (`USERNAME="${DOME_USER:-${USER:-robot}}"`), never `manifest/user.txt`, but the documented workflow never exports `DOME_USER`, only writes it into `manifest/user.txt`. Under `sudo`, `$USER` is `root`, so `DOME_DIR=/home/root/dome-docker` was wrong and `grep: /home/root/dome-docker/manifest/user.txt: No such file or directory` followed (hit live during VM bring-up). Fixed to read `manifest/user.txt` then `manifest/config.txt` default, same env > user.txt > config precedence as `bare-metal-build.sh`/`dome-config.sh`. Verified by resolving against the real manifest files (got `pitosalas`, not `root`). No automated test — same Linux-only constraint as the IP-printing chore above.
- [x] Remove bogus `dome-docker` entry from `manifest/packages.txt` `[ros]` section — introduced in commit `e158702` with no stated rationale, it made `bare-metal-base.sh` apt-install a nonexistent `ros-kilted-dome-docker` package, failing live during VM bring-up (`E: Unable to locate package ros-kilted-dome-docker`). Not a real ROS package, just the repo's own name accidentally left in the list. Regression test: `tests/test_f01_manifest.sh` "packages.txt [ros] has no bogus self-referential entry" check.
- [x] Removed time estimates ("10-30 min", "20-60 min", "10-15 min") from `02-doc/shell-howto.md`/`docker-howto.md`/`README.md` per explicit user feedback — durations vary too much across Pi/VM/network to be meaningful, and a wrong number is worse than none.
- [x] Fix `manifest/bashrc` sourcing `~/rosutils/ros2_robot_bashrc.bash` without exporting `ROS_DISTRO` first — that script requires it set and errors (`ROS_DISTRO: ERROR: ROS_DISTRO must be set before sourcing ros2_robot_bashrc.bash`, `bash: /opt/ros//setup.bash: No such file or directory`), hit live in a new terminal after `bare-metal-build.sh`. Fixed by exporting `ROS_DISTRO` from `~/dome-docker/manifest/config.txt` as the first line of `bashrc`, before the `source`. Regression test: `tests/test_f01_manifest.sh` "bashrc exports ROS_DISTRO before sourcing rosutils" check (verifies export precedes the source line).
- [x] Added VS Code (`[vscode]` section, Microsoft's official apt repo) to `manifest/apt-repos.txt`, same pattern as `doppler`/`github-cli`. Target is Ubuntu Desktop (not headless Server, per user — that's the ISO actually used for the VM scenario), so a GUI editor is applicable. Universal (not `DOME_TARGET`-gated) — apt-installing `code` is harmless on a headless target even if there's no GUI to run it. Updated `README.md`/`02-doc/notes.md`/`02-doc/spec.md`/`02-doc/shell-howto.md` mentions of the apt-repos.txt third-party list; added `vscode` to the `test_f01_manifest.sh` apt-repos.txt section-field loop.
- [x] `README.md` "Local Configuration" section read as a universal first step (create `manifest/user.txt` + source `dome-config.sh`), but it's actually Docker-scenario-only — scenarios 1/2 create `user.txt` on the target in `shell-howto.md` Step 2 instead. Confused a live Pi microSD install (user unsure what to do first). Reordered README: new "Getting Started" section up top routes by scenario to the right Step 1 before any other section; "Local Configuration" moved after it and labeled Docker-only. Doc-only, no test.
- [x] `claude-code`'s installer refuses to run as root (`Error: do not run this installer with sudo` — it installs into `$HOME`, and under `bare-metal-base.sh`'s all-root execution that would be `/root`, not the target user's home). Whole script runs `set -euo pipefail` as root; hit live once `mcfly`'s `--force` fix let execution reach the `claude-code` entry. `bare-metal-base.sh` previously never resolved `DOME_USER` at all (didn't need to — apt/pip/mcfly all install system-wide). Fixed: resolve `DOME_USER` with the same env > user.txt > config precedence used in `bare-metal-build.sh`/`host-setup.sh`; added an optional `run_as_user = true` field to `manifest/tools.txt` entries, honored by running that tool's installer via `sudo -u "${DOME_USER}"` instead of as root. Set on `[claude-code]` and `[kimi-code]` (both install into `$HOME`); `[mcfly]` stays root (installs to `/usr/local/bin`, system-wide). Regression tests: `tests/test_f01_manifest.sh` checks the script reads `run_as_user` and can `sudo -u`, and that both entries have it set.
- [x] `mcfly`'s installer refuses to overwrite an existing binary without `--force` and exits nonzero; `bare-metal-base.sh` runs under `set -euo pipefail`, so a rerun of the script (already installed once) died at the `[6/8]` curl-tools step on `mcfly` specifically — before ever reaching later `tools.txt` entries (`claude-code`, `kimi-code`). Hit live: those two appeared to silently fail to install, but they'd never even been attempted. Fixed by adding `--force` to `[mcfly]`'s `args` in `manifest/tools.txt`, matching the "safe to re-run" claim already documented for this script elsewhere. Not generalized to a continue-on-error loop for all `tools.txt` entries — that changes failure semantics for genuine install errors and wasn't asked for; flagging here in case a future tool addition hits the same one-bad-tool-blocks-all-later-tools shape.
- [x] Added `claude-code` and `kimi-code` to `manifest/tools.txt` (curl-installed CLIs, same pattern as `mcfly`). Both installers pipe to `bash` (per each tool's documented install command), but `tools.txt`'s only method, `curl-sh`, pipes to `sh` — extended `bare-metal-base.sh`'s tool installer with a new `curl-bash` method rather than mismatching interpreters. Regression tests: `tests/test_f01_manifest.sh` asserts both `tools.txt` sections exist and that `bare-metal-base.sh` handles `curl-bash`. While adding the assertions, found `assert_field()` itself was broken for any bracketed key (`[mcfly]`, `[claude-code]`) — `grep -q "^${key}"` treats an unquoted `[...]` as a regex character class, not a literal match; `[mcfly]` "passed" only by accident (`m` fell inside the resulting class), and `[claude-code]` hit an invalid descending range (`e-c`) and errored outright. Fixed `assert_field` to use `grep -qF` (fixed-string match) — the semantics every caller actually intended.
- [x] Removed `[kimi-code]` from `manifest/tools.txt` per user request — dropped as a standard provisioned component, so `bare-metal-base.sh` no longer curl-installs it. `[mcfly]` and `[claude-code]` remain. Updated `tests/test_f01_manifest.sh` to stop asserting the `[kimi-code]` section and to drop `kimi-code` from the `run_as_user=true` check loop (now `claude-code` only). Earlier chore-log entries that mention `kimi-code` are left as-is — they are a historical record of when it was added. Config-only change, no behavior spec affected.
- [x] `manifest/repos.txt`'s `[ros_ws]` section entries had 2-space leading indentation (from the same uncommitted edit that added `dome_mission`/`dome_nav`/etc.), but `bare-metal-build.sh`'s `clone_section` extracts entries via `awk ... f && /^[^#[:space:]]/`, which requires the first character to be non-whitespace. Every indented line failed that match, so the *entire* `[ros_ws]` section silently produced zero repos on `git pull` + rerun on the Pi — not just the new entries. Existing packages on disk (from before the indentation was introduced) masked the bug; only the newly-added repos (`dome_nav` etc.) exposed it, since they had never been cloned. Fixed by removing the indentation, restoring the flat style used by every other section (`[root]`, `[root-pi]`, `[uros_ws]`). Regression test: `tests/test_f01_manifest.sh` "repos.txt entries parse via clone_section's awk extraction" — runs the exact extraction awk per section and asserts entry count > 0 (would have caught this immediately; missed at `/checkpoint` time because existing style/format checks didn't exercise the clone-time extraction path).
- [x] `bare-metal-build.sh`'s `clone_section` had no skip-existing check — `git clone` into an already-cloned, non-empty dest dir fails, and `set -e` + the explicit `|| exit 1` trap meant the whole script died at the *first* already-cloned repo on any rerun, never reaching new repos added later in `manifest/repos.txt`. Hit live: after adding `dome_nav`/etc. to the manifest, rerunning the script to pick them up would've failed immediately at `better_launch` (first entry, already cloned) instead of reaching the new repos further down the list. Fixed: `clone_section` now checks `[[ -d "${base_dir}/${dest}" ]]` and skips with a log line instead of attempting the clone. Regression test: `tests/test_f01_manifest.sh` "clone_section skips already-cloned repos" (static grep — real clone behavior needs network/root, consistent with other bare-metal script checks in this suite).
- [x] `bare-metal-build.sh` hard-required `colcon.txt`'s `flags` field to be non-empty (`ERROR: flags not set` + `exit 1`), but an empty `flags` is a legitimate choice — full (non-symlink) colcon install, which the user intentionally switched to by deleting the `flags = --symlink-install` line. Caught by `test_f01_manifest.sh`'s "colcon.txt required fields" check during `/checkpoint`. Fixed: removed the non-empty requirement from `bare-metal-build.sh` (empty `flags` just expands to nothing in the `colcon build` call) and dropped the corresponding `assert_field ... "flags"` from `test_f01_manifest.sh` — `packages_skip` is still required. Regression test: existing suite now passes with `flags` absent from `colcon.txt`.
- [x] Split `02-doc/shell-howto.md` (interleaved Pi/VM steps, Primary/Target + Pi:/VM: tags every step, quick-reference diff table) into two single-thread guides: `02-doc/pi-howto.md` and `02-doc/vm-howto.md`, each self-contained start to finish with no scenario-switching mid-document, repeating shared steps (host-setup, bare-metal-base, bare-metal-build) verbatim rather than cross-referencing. Deleted `shell-howto.md`. Updated live cross-refs in `README.md`, `02-doc/howto.md` (Guide row + intro paragraph), `02-doc/docker-howto.md`. Left historical mentions in `02-doc/current.md` session log, `04-tasks/chores.md` (this file), `04-tasks/notdone/TF03-vm-setup-support.md`, `03-features/notdone/f03-vm-setup-support.md`, and closed issue `I01` untouched — they record what was true when written, not live pointers. Doc-only, no test.
- [x] `scripts/host-setup.sh`'s final "Host setup complete. Log out and back in for docker group membership to apply." line ran unconditionally, even under F05's new `DOME_MODE=native` (where no `usermod -aG docker` ever happens) — surfaced live on a Pi rerun right after landing F05, user asked why a docker-group message showed up on a native run. Fixed: message now only mentions docker group membership when `DOME_MODE=docker`; native mode just prints "Host setup complete." Covered by existing `tests/test_f05_dome_mode.sh` syntax/behavior checks (no new test needed — pure string branch, not new gated behavior).
- [x] Added `git@github.com:Boston-Robot-Hackers/j3.git` (dest `j3`) to `manifest/repos.txt` `[root]` section, per explicit user request — clones straight into `$DOME_HOME` (same as `rosutils`/`depthai-python`/`linorobot2_hardware`), not into `ros2_ws/src`. No script change needed, `clone_section` already handles `[root]` generically. Full suite still passes (59+43+28+12+7, 0 failed) — no test asserts an exhaustive `[root]` list, only that specific named repos are present/absent.
- [x] Added `git@github.com:Boston-Robot-Hackers/dome_telemetry.git` (dest `dome_telemetry`) to `manifest/repos.txt` `[ros_ws]` section — the repo existed and was reachable but was never in the manifest, so it was never cloned or built. Nothing caught it: the only reference to the bare `dome_telemetry` package is a *runtime* `bl.include("dome_telemetry", "robot.launch.py")` in `dome2/launch/gendrv.launch.py`, not a `package.xml` dependency, so `rosdep install` saw no missing key and `colcon build` exited 0 on a Pi that was silently missing the package — failing only at launch time. Distinct from `dome_telemetry_msgs`, which is a package *inside* the already-cloned `dome_vision` repo and resolves fine (that's what `dome_control`/`dome_vision_ros` actually depend on). Verified the repo is a single ament_python package at root (`<name>dome_telemetry</name>`) with `launch/robot.launch.py`, default branch `main`, so no branch field needed. Regression test: none added — catching this class of gap means parsing `bl.include(...)` calls across ~20 sibling repos and cross-checking `repos.txt`, machinery that would live in the wrong repo for one missing line; existing `test_f01_manifest.sh` `[ros_ws]` extraction check covers that the new entry parses (20 entries, was 19).
- [x] `02-doc/pi-howto.md` "Development Cycle" section says `sudo scripts/bare-metal-build.sh   # re-clones changed repos, rebuilds workspace` — it never re-clones. `clone_section` skips any dest dir that already exists (deliberate, see the skip-existing chore above), so a rerun picks up *newly added* repos and rebuilds the workspace, but never pulls updates into the ~20 already-cloned repos. Following the documented cycle after a `git pull` in dome-docker leaves every ROS repo at whatever commit it was first cloned at. Decide whether to fix the doc (state that updating cloned repos is manual) or the behavior (add an opt-in pull pass) — the latter is a behavior change and would need a feature/task pair, not a chore. Same wording to check in `02-doc/vm-howto.md`. Resolved by fixing the doc: both howtos now say the script only clones new repos and show the manual `git -C <repo> pull` step. Behavior unchanged; an opt-in pull pass would still need a feature/task pair. Doc-only, no test.
- [x] `02-doc/pi-howto.md` "colcon build fails" troubleshooting block tells the user to run `colcon build --symlink-install --packages-skip depthai_rospi`, but `--symlink-install` was intentionally removed from `manifest/colcon.txt` (see the `flags` chore above — full non-symlink install is the deliberate choice). Following the troubleshooting recipe produces a workspace built differently from the one `bare-metal-build.sh` produces. Doc-only fix: dropped `--symlink-install` from the recipe in `pi-howto.md` and `vm-howto.md` (same block) so it matches the manifest. No test — doc-only.
- [ ] `scripts/host-setup.sh` is the only script that doesn't source `manifest/lib.sh` — it re-implements config parsing inline with raw `grep | cut` (`DOME_USER`, `DOME_TARGET`, `DOME_MODE`) and falls back to hardcoded defaults (`:-robot`, `:-pi`, `:-native`) instead of `manifest_config`'s fail-fast. A typo'd or missing key in `config.txt` errors loudly in `bare-metal-base.sh`/`bare-metal-build.sh` and silently defaults in `host-setup.sh`. Inconsistent with the "scripts are thin executors over `manifest/`, shared helpers in `lib.sh`" design in `02-doc/notes.md`. Related: the three-level env > user.txt > config.txt cascade is duplicated verbatim ~9 times across the three scripts — a `manifest_resolve <key>` helper in `lib.sh` would collapse all of them (style guide's DRY rule flags repeated 3+ line sequences).
- [ ] `scripts/bare-metal-build.sh` provenance echo can't report what it claims: it prints `env=${DOME_USER:-}` but `DOME_USER` was overwritten by the cascade one line earlier, so it always echoes the *resolved* value and makes every run look like the env var was set. Defeats the purpose of the "(from: env=... file=... default=...)" diagnostic. Fix by capturing the original `${DOME_USER:-}` into a temp before the cascade assignment. Cosmetic but actively misleading when debugging which source won.
- [x] Rewrote `README.md` for end users per explicit user request — it opens with what the project does ("sets up a Dome robot from scratch"), what you end up with, how to pick a scenario, and the three everyday commands, with implementation detail cut to a single closing pointer section. Removed the `scripts/`, Dockerfiles, `manifest/`, and directory-layout tables wholesale; those describe how it works, not how to use it, and live in `02-doc/`. Deliberately names no config flags (`DOME_TARGET`, `DOME_MODE`) in user-facing text. This broke `tests/test_f03_vm_target.sh`'s "README documents VM setup" check, which asserted the literal string `DOME_TARGET=vm` in `README.md`. Replaced it with two assertions that preserve the intent without requiring flag names in an end-user doc: README links to `vm-howto.md`, and `vm-howto.md` documents `DOME_TARGET=vm`. Net stronger — flag-level doc coverage moved to the file that should own it, and the flag's *behavior* was already covered separately by the config.txt default, per-script reference, and `[apt-pi]`/`[pip-pi]`/`[root-pi]` gating checks.
- [x] Added a "Migrating A Pi/VM Set Up Before The Repo Rename" section to `02-doc/pi-howto.md` and `02-doc/vm-howto.md`, plus a symptom-keyed Troubleshooting entry pointing at it. Covers the failure hit live during the `dome-docker` → `provision_dome` rename: `manifest/bashrc` is a *template* that only reaches `~/.bashrc` when `bare-metal-build.sh` copies it (`install ... "${MANIFEST_DIR}/bashrc" "${DOME_HOME}/.bashrc"`), so a `git pull` updates the template while the live `~/.bashrc` keeps pointing at the old directory — `ROS_DISTRO` resolves empty and every new shell dies on `/opt/ros//setup.bash: No such file or directory`. Fix is `cp ~/provision_dome/manifest/bashrc ~/.bashrc`, not a full `bare-metal-build.sh` rerun (which would sit through rosdep and a full colcon build on ARM to reach the one line that matters). Troubleshooting entry is keyed on the error string with its distinctive empty path segment, since that is what someone will actually search for.
- [x] Repointed the ~12 absolute paths in `.claude/settings.local.json` from `/Users/pitosalas/mydev/dome-docker/` to `/Users/pitosalas/mydev/provision_dome/` after the local directory rename — stale rules silently stop matching and re-prompt for previously allowed commands. Also dropped two rules referencing a prior session's scratchpad UUID (`.../ab6a9a72-.../scratchpad/...`); those encode a per-session directory and could never match again regardless of the path fix, so repointing them would have been theatre.
- [x] `02-doc/vm-howto.md` Step 5's bullet list ("Install `manifest/bashrc`
  and `bru` symlink") never named `ROS_DISTRO` or explained the mechanism —
  a reader hit `echo "$ROS_DISTRO"` working in Step 6's smoke test with no
  idea why, since the actual chain (`manifest/config.txt` → exported by
  `manifest/bashrc` → installed as `~/.bashrc` by `bare-metal-build.sh`)
  wasn't spelled out until the much-later "Migrating..." section. Checked:
  the mechanism itself is correct and already tested (export precedes the
  `rosutils` source, per the `manifest/bashrc` chore above) — this was a
  doc-clarity gap only, no behavior bug. Fixed by expanding the Step 5
  bullet to name `ROS_DISTRO` and the config.txt→bashrc→~/.bashrc chain
  explicitly. `02-doc/pi-howto.md` has the identical wording gap (shared
  origin, see the shell-howto.md split chore) — not fixed here since the
  user scoped this to the VM doc; flagging for a follow-up chore.
- [x] `manifest/pip.txt` lists `numpy` unpinned in `[pip]`, which is the dome-docker-side enabler for the `dome_vision` breakage tracked in `02-doc/current.md` — `bare-metal-build.sh`'s separate per-package `pip3 install` of `dome_vision` doesn't share the base script's combined resolver context, so an unpinned `numpy` there silently upgraded to 2.5.1 and broke `depthai-sdk`'s hard `numpy<2.0.0`. The tracked fix is in the `dome_vision` repo (pin `numpy<2`, drop duplicate `opencv-python`); consider whether `pip.txt` should also pin `numpy<2` defensively so the constraint holds regardless of what a cloned repo's `pyproject.toml` asks for. Depends on the `dome_vision` fix landing first — revisit after. Closed without a change: a `pip.txt` pin would not help, since `pip.txt` is read only by the base script's single combined install (which already resolves numpy to 1.x via `depthai-sdk`); the breakage came from the later, separate per-package `pip3 install` in `bare-metal-build.sh`, which never reads `pip.txt` and could upgrade over any pin. The real fix is in the `dome_vision` repo (pin `numpy<2`, drop duplicate `opencv-python`). A `PIP_CONSTRAINT` file in the build script would be the defensive option, but is a behavior change needing a feature/task pair — not pursued.
- [x] `oci-howto.md` / `cloud-howto.md` doc fixes from the TF07.0 findings: (1) the host GitHub key must belong to the user that runs the build, use the default `~/.ssh/id_ed25519` name, and not be deleted before the build (else `Permission denied (publickey)` at the first private clone); (2) first-login now shows the explicit `ssh -i … -o IdentitiesOnly=yes` form, since a Mac `Host *` `IdentityFile` masked the instance key. Both guides were checked. Doc-only, no test. Superseded for cloud hosts once F10 removes the key requirement.
- [x] Switched `manifest/repos.txt`'s `rosutils` entry from `git@github.com:campusrover/rosutils.git … PRIVATE_REPO` to `https://github.com/campusrover/rosutils.git rosutils` (no marker), per explicit user request — the repo is public (verified anonymous `git ls-remote` over HTTPS succeeds), so an HTTPS clone is read-only and needs no push credential. Two effects: (1) rosutils now clones on a `DOME_CLONE_OVERRIDE=PUBLIC_ONLY` build (a cloud dev host) instead of being skipped as private, giving those boxes `bru`/`ros2_robot_bashrc.bash` without any GitHub key; (2) all targets (pi/vm/docker) now fetch rosutils anonymously over HTTPS rather than via SSH — strictly fewer credentials needed, same content. Verified via `manifest_should_clone PUBLIC_ONLY false <https-url>` → clones. Config-only; covered by `test_f01_manifest.sh`'s `[root]`-section parse checks (no test asserts rosutils' transport specifically).
- [x] `manifest/bashrc` never sourced the `~/ros2_ws/install` overlay when `~/rosutils/ros2_robot_bashrc.bash` was present — the overlay `source` lived only in the `else` branch, guarded by a now-false comment ("rosutils is a PRIVATE_REPO, absent under PUBLIC_ONLY"). The rosutils→HTTPS chore above made rosutils clone even under `PUBLIC_ONLY`, so the `if` branch runs, `ros2_robot_bashrc.bash` (a bru-oriented file the user no longer uses) does not source the local workspace, and freshly-built packages were missing from `ros2 pkg list` until sourced by hand. Hit live on the OCI dev host bring-up. Fixed by moving the `[[ -f ~/ros2_ws/install/setup.bash ]] && source` out of the `else` so it always runs after base ROS is sourced, and dropping the stale comment. Existing `test_f01_manifest.sh` "bashrc exports ROS_DISTRO before sourcing rosutils" check still holds; the always-source line is a static one-liner (real sourcing needs a built workspace). Open follow-up: since the user dropped `bru`, decide whether to keep sourcing `ros2_robot_bashrc.bash` at all.
- [x] `manifest/packages.txt` `[apt-desktop]` installed `tigervnc-standalone-server` but not `tigervnc-tools`, which provides the `tigervncpasswd` binary `vncserver` calls to set the desktop password — so `DOME_DESKTOP=vnc` + `scripts/start-desktop.sh` died with `vncserver: Couldn't find "tigervncpasswd" on your PATH`. Hit live on the OCI dev host. Fixed by adding `tigervnc-tools` to `[apt-desktop]`. Covered by `test_f01_manifest.sh`'s `[apt-desktop]` section-parse check (asserts the section is non-empty/parseable, not an exhaustive package list).
- [ ] (absorbed by F14 / TF14.6) `manifest/packages.txt` `[apt-desktop]` installs the `xfce4` metapackage but no terminal emulator, so a fresh vnc desktop throws `Failed to execute default Terminal Emulator` / `Input/output error` on first terminal launch — confirmed live on the OCI box 2026-09-25. Add `xfce4-terminal` plus the agreed app list to `[apt-desktop]`; tracked as F14 desktop-completeness rather than a standalone fix.
- [ ] (operational, not code) Live box `dome-cloud-1` was provisioned before `tigervnc-tools` (`a218d0f`) and hasn't rerun `bare-metal-base.sh`, so `tigervncpasswd` was missing until hand-installed 2026-09-25 — the exact gap that chore fixed. Reconcile the box with `sudo scripts/bare-metal-base.sh` (or rebuild). Manifest is already correct for fresh builds.
- [ ] The env > user.txt > config.txt cascade silently concatenates duplicate keys instead of erroring. Every reader uses the same `grep '^KEY=' user.txt | cut -d= -f2 | tr -d '[:space:]'` shape (`bare-metal-base.sh:29`, `scripts/start-desktop.sh:18-19`, and the ~9 duplicated cascades noted in the `host-setup.sh`/`lib.sh` chore above). With two matching lines `grep` emits two, `cut` emits two, and `tr -d '[:space:]'` deletes the *separating newline* — so `DOME_DESKTOP=vnc` twice parses as `vncvnc`, and `bare-metal-base.sh:99`'s `== "vnc"` test then fails, silently skipping all `[apt-desktop]` packages. Hit live on `dome-cloud-1` 2026-09-26: `manifest/user.txt` had `DOME_DESKTOP=vnc` twice (the `printf ... >>` append in `cloud-howto.md` Step 7 is run-twice-unsafe), so a rerun of the base script would have installed no desktop while reporting success. Reproduced locally. Worked around by rewriting the box's `user.txt`; the parser is still wrong. Fix: make the resolver read the *last* match (`tail -1`) or, better per the style guide's "report, don't guess" rule, fail loudly on a duplicate key — and fold it into the single `manifest_resolve` helper the DRY chore above already calls for. Regression test: feed a duplicate-key `user.txt` to the resolver and assert it errors rather than concatenating. Also worth making the howto's `printf >>` steps idempotent.
